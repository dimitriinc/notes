Common source of security vulnerabilities - different modules of a system understand the same data in an ambiguous way.

There is a balance play between convenience and security: if a feature introduces a convenience in user experience (or devs') it might do it on the expense of security.